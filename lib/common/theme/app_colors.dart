import 'package:flutter/material.dart';

class AppColors{
  static const bgColor = Color(0xFFefefef);
  static const primaryColor = Colors.indigo;
  static const secondaryColor = Colors.grey;
  static const errorRed = Colors.redAccent;
  static const appWhite = Colors.white;
  static const secondaryBlack = Color(0xFF201F1F);
}